
import { GameSettings } from './types';
export const DEFAULT_SETTINGS: GameSettings = {
  maxPlayers: 10,
  teamSize: 5,
  rounds: 10,
  pointsCorrect: 10,
  pointsWrong: -5,
  questionSeconds: 15,
};
